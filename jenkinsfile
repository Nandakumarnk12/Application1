FROM maven AS build
WORKDIR /mavendir
COPY . /mavendir
RUN mvn clean install

FROM openjdk:17-alpine
WORKDIR /jdkdir
COPY --from=build /mavendir/target/app-0.0.1-SNAPSHOT.war /jdkdir
ENTRYPOINT ["java","-jar","app-0.0.1-SNAPSHOT.war"]
EXPOSE 8080
